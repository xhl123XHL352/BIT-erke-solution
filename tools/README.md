# 工具使用说明

本目录包含获取Token和验证Token的相关工具。

## 🔍 获取Token

由于课程系统是微信小程序，需要使用Fiddler在电脑微信中抓包获取Token。

### 推荐方法：使用Fiddler抓包

**详细步骤请查看：** [`fiddler_capture_guide.md`](fiddler_capture_guide.md)

**快速开始：**

1. **安装Fiddler**
   - 下载：https://www.telerik.com/fiddler
   - 安装Fiddler Classic

2. **配置Fiddler和系统代理**
   - 以管理员身份运行 `setup_fiddler.bat`
   - 或按照 `fiddler_capture_guide.md` 手动配置

3. **在微信中打开小程序**
   - 在Fiddler中过滤 `qcbldekt.bit.edu.cn`
   - 在微信中访问课程列表
   - 在Fiddler中找到请求，复制Authorization头

4. **保存Token**
   - 将Token保存到 `token.txt`
   - 或直接配置到抢课系统

### 自动提取Token（可选）

可以使用FiddlerScript自动提取Token：

1. 在Fiddler中：`Rules` → `Customize Rules...`
2. 参考 `fiddler_auto_extract.js` 中的代码
3. 修改文件路径为你的实际路径
4. 保存脚本（Ctrl+S）

## ✅ 验证Token

捕获Token后，可以使用验证脚本验证Token是否有效：

**Windows:**
```bash
verify_token.bat
```

**Linux/Mac:**
```bash
chmod +x verify_token.sh
./verify_token.sh
```

**或直接运行:**
```bash
python verify_token.py
```

验证脚本会：
- 自动读取 `token.txt` 文件
- 测试Token是否有效
- 显示课程列表（如果Token有效）
- 给出明确的验证结果

## 📁 文件说明

- `fiddler_capture_guide.md` - Fiddler抓包详细指南（**必读**）
- `fiddler_auto_extract.js` - Fiddler自动提取Token脚本
- `setup_fiddler.bat` - Fiddler配置助手（自动设置系统代理）
- `remove_proxy.bat` - 清除系统代理设置
- `verify_token.py` - Token验证脚本
- `verify_token.bat/sh` - 验证脚本启动文件
- `token.txt` - 保存捕获的Token（自动生成）

## ⚠️ 注意事项

1. **使用完毕后记得清除代理**
   - 运行 `remove_proxy.bat` 清除系统代理
   - 或手动关闭系统代理设置

2. **Token格式**
   - Token格式通常是：`Bearer xxxxx`
   - 复制完整内容，包括"Bearer "前缀

3. **Token有效期**
   - Token可能会过期
   - 如果验证失败，需要重新抓包获取新Token

4. **Fiddler证书**
   - 必须将Fiddler证书安装到系统
   - 否则无法解密HTTPS请求

## 🔧 故障排除

### Fiddler中看不到微信请求

1. 确认系统代理已设置（127.0.0.1:8888）
2. 完全关闭微信后重新启动
3. 检查Fiddler是否在运行
4. 确认Fiddler中已启用HTTPS解密

### 微信无法连接网络

1. 检查系统代理设置
2. 确认Fiddler正在运行
3. 重新安装Fiddler证书
4. 重启微信

### Token验证失败

1. 检查Token格式是否正确
2. 确认Token未过期
3. 重新抓包获取新Token

更多问题请查看主README的故障排除部分。

祝使用愉快！🎉
